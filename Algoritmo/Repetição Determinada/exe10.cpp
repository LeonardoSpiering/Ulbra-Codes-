#include<stdio.h>
#include<conio.h>
main()
{
	int x, d, f, cont=10<=20;
	
	for(x=1; x<=10; x++)
	{
		printf("\nDigite o valor:");
		scanf("%d", &d);
	}
		if(d= 10<=20)
			cont++;
			printf("\nA quantidade de numeros dentro de 10 a 20 sao: %d", cont);
	
		if(d!=10<=20)
			cont++;
		{
		printf("\nA quantidade de numeros fora de 10 a 20 sao: %d", cont);
		}
}
